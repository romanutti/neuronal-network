function cm = confusionmatrix(pred, y)
%CONFUSIONMATRIX Caluculates the confusion matrix for given predictions
%   cm = CONFUSIONMATRIX(pred, y) computes the confusion matrix for given
%   predictions

cm = confusionmat(y,pred)

% =========================================================================


end
